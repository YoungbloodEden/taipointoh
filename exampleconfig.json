{
  "info": {
    "token": "<bot auth token here> Changes to the framework now require the bot to login with an authtoken from the website, or via a bot account registered with discord.",
    "prefix": "<prefix here> (Currently only supports single prefix ie. $, ^, !, Z)",
    "apiKey": "<Google API Key Here> For YouTube Functionality. Make sure your key has access to the YouTube API"
  }
}
